# SPDX-License-Identifier: MPL-2.0
name: backup_scheduler
description: Platform-neutral scheduling policy spike for user-selected Localhold backups.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.8
